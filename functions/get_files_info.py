import os

def get_files_info(working_directory, directory="."):
    
    #if working_directory == os.path.abspath()
    return os.path.abspath(working_directory)

def working_directory(working_dir_abs, directory) :
return target_dir

